# Changelog

All notable user-visible behavior changes are recorded here. Format
is loosely based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

变更历史正在迁移到本文件。当前阶段请通过 `git log` 查看完整变更：

```bash
git log --oneline --decorate
```

后续版本会按 Keep a Changelog 规范在此记录每次发布的用户可见行为变更。
