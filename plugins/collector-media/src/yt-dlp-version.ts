/**
 * Pinned yt-dlp version (bump after re-running integration tests).
 *
 * Do NOT add a type annotation: scripts/inject-yt-dlp-version.mjs parses this
 * line via regex that requires the literal form `YT_DLP_PINNED_VERSION = '…'`.
 */
export const YT_DLP_PINNED_VERSION = '2026.03.17';
