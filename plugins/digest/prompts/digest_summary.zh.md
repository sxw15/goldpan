输入快照（JSON）：
{{snapshotJson}}

输出 JSON 对象，只能包含以下字段：
- "headline"：一句话概括（≤ 40 个汉字）
- "bullets"：2-5 条一句话高光，必须引用快照里实际出现的条目（优先 findings 与 thoughts）
- "closing"：可选，一句话展望（可为空字符串）

规则：
- 不要编造快照里没有的内容
- 优先使用具体的标题 / URL，不要泛泛而谈
- 语气中立，不使用表情符号

只输出 JSON 对象，不要附加任何其它文字。
